# Project Memory — Pointer

The decision log for this project lives in **[docs/DECISIONS.md](DECISIONS.md)**.

This file previously held five dated decisions about the trust model, artifacts,
and redaction. They were merged into `D-000` in `docs/DECISIONS.md` on
2026-09-26, which is now the single canonical record. Nothing was lost: the
original entries are preserved there in substance, with their reasoning, and the
file above states where they are enforced and which test pins each one.

Do not add new decisions here. Add them to `docs/DECISIONS.md` instead, so there
is exactly one place to look and exactly one place to review.
